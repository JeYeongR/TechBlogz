# PRD: TechBlogz (필터링 기능이 있는 개인용 RSS 리더)

## 1. 배경 및 문제 정의

현재 개발/AI 관련 아티클을 여러 소스에서 개별적으로 확인하고 있음:

- 단발성 아티클: Anthropic 블로그, Lilian Weng, applied-llms.org, hamel.dev
- 정기 구독 소스: Simon Willison, 국내 기술 블로그(oliveyoung.tech, techblog.woowahan.com, toss.tech), GeekNews

기존 무료 RSS 리더(Feedly, Inoreader)로 피드 수집 자체는 해결 가능하지만, "지금 관심 있는 주제와 관련된 글"을 우선적으로 골라주는 기능은 없음. 매번 전체 목록을 훑어야 하는 비효율이 있음.

## 2. 목표

- 등록한 RSS/Atom 피드를 자동으로 수집
- 읽음/안읽음 상태 관리
- LLM 기반 태그 분류/관심사 매칭 스코어링으로 우선순위 정렬 (MVP2)

### Non-Goals (이번 범위 제외)

- 멀티유저 지원 (개인용)
- 뉴스레터(이메일) 직접 수집 — 필요 시 Kill the Newsletter 같은 외부 도구로 RSS 변환 후 연동
- 모바일 네이티브 앱

## 3. 사용자 시나리오

1. 아침에 리더를 열면 등록된 피드에서 새로 올라온 글 목록이 보인다.
2. 관심사와 관련도가 높은 글이 상단에 노출된다.
3. 읽은 글은 읽음 처리되어 목록에서 흐리게 표시되거나 숨겨진다.
4. 새 피드는 `feeds.json` 파일에 URL을 추가해 코드로 커밋하고, 배포 시 반영된다.

## 4. 기능 요구사항

### 4.1 피드 관리
- 피드 목록은 저장소의 `feeds.json` 파일로 관리 (URL, 이름 등)
- 새 피드 추가/삭제는 `feeds.json` 수정 후 커밋 → 배포 시 반영 (UI 입력 폼 없음)
- 배포 시 `feeds.json` 기준으로 유효성 검증 (파싱 가능 여부 확인)
- 피드별 마지막 수집 시각 표시

### 4.2 피드 수집
- 주기적 폴링 (기본 1시간 간격, 추후 조정 가능)
- 신규 글만 저장 (중복 방지 — guid 또는 link 기준)

### 4.3 글 목록 / 뷰어
- 카드형 그리드 레이아웃 (이미지 썸네일 + 제목 + 요약 + 출처/날짜)
- 썸네일 없는 피드는 출처 블로그 파비콘 또는 플레이스홀더로 대체
- 최신순 정렬 기본
- 읽음/안읽음 상태 토글 (읽은 카드는 흐리게 표시)
- 원문 링크로 이동 (새 탭)
- 무한 스크롤 또는 페이지네이션으로 카드 넘겨보기

### 4.4 필터링/태그 분류 (LLM, MVP2)
- 신규 글의 제목+요약을 LLM에 전달해 관심 토픽 태그 분류 + 관련도 점수 산출
- 관심 토픽은 사용자가 텍스트로 정의 (예: "LLM 에이전트, RAG, 파인튜닝")
- 태그/점수 기준 정렬 및 필터링 옵션 제공
- (제거됨) 키워드 포함/제외 규칙 기반 필터링 — MVP1에서 시도했으나 위 LLM 방식으로 대체

## 5. 기술 스택 (확정)

| 영역 | 선택 |
|---|---|
| 배포 | Vercel |
| 프레임워크 | Next.js |
| UI | shadcn/ui + Tailwind CSS |
| 피드 파싱 | rss-parser (npm) |
| DB | Supabase (Postgres, 무료 티어) — GitHub Actions 크론이 15~30분마다 호출하므로 7일 비활성 자동 일시정지 이슈 없음 |
| 스케줄링 | GitHub Actions 스케줄 크론 → Next.js API Route 호출 (무료 최대치로 15~30분 간격) |
| LLM 스코어링 | Gemini API (기존 url-summarizer 프로젝트와 동일 방식 재사용), 신규 글 저장 시 1회만 계산 후 DB에 캐싱 |

## 6. 마일스톤

### MVP 1
1. 피드 등록 + 수집 + 목록 표시 (필터링 없음)
2. 읽음 상태 관리

### MVP 2
3. LLM 기반 태그 분류 + 관련도 스코어링 및 정렬/필터링

## 7. 결정된 운영 정책

- **폴링 주기**: GitHub Actions 무료 스케줄 크론 사용, 15~30분 간격 (Actions 스케줄 지연 감안)
- **LLM 스코어링 비용 제어**: 신규 글 최초 저장 시점에 1회만 스코어링, 결과는 DB에 캐싱하여 재계산 없음
- **호스팅**: Vercel (프론트+API), DB는 Supabase 무료 티어, 크론 트리거는 GitHub Actions

## 8. UI/UX 방향

- **컴포넌트**: shadcn/ui + Tailwind CSS — 빠른 개발, 표준 컴포넌트 재사용
- **레이아웃**: 카드형 그리드 (이미지/요약 포함), 넘겨보기 방식
- **카드 구성 요소**: 썸네일, 제목, 요약 1~2줄, 출처, 발행일, 읽음 여부
- **썸네일 처리**: RSS `<enclosure>` 또는 og:image 우선 사용, 없으면 출처 파비콘/플레이스홀더 대체
- **정보 밀도**: 개인용 읽기 도구 성격에 맞춰 텍스트 가독성과 스캔 속도 우선, 과도한 장식 지양

## 9. 인증

- 로그인/회원가입 기능 없음. 개인용 단일 사용자 도구로 배포 URL을 공개해도 접근을 열어두는 방식
- Supabase는 DB 용도로만 사용, Supabase Auth는 사용하지 않음
